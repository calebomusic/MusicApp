<h1>Bands</h1>
<ul>
<% @bands.each do |band| %>
  <li>
    <%= link_to "#{band.name}", band_url(band) %>
  </li>
<% end %>

<br>
</ul>
<h3>
  <p>
    <%= link_to "Create a Band!", new_band_url %>
  </p>
</h3>
